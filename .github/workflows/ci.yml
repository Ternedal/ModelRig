name: ci

# Runs the shared test suite (_tests.yml) on every push to main and on pull
# requests. The temporary materializer job below exists only on PR #36 and is
# removed before the scheduler product commit is created.
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: write

jobs:
  materialize-scheduler:
    if: github.head_ref == 'build/scheduler-approval-token-inspect'
    uses: ./.github/workflows/scheduler-ed25519-self-build.yml

  test:
    uses: ./.github/workflows/_tests.yml

  android-compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - uses: actions/setup-java@0f481fcb613427c0f801b606911222b5b6f3083a # v5
        with:
          distribution: temurin
          java-version: "21"
      - uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4
      - name: Install platform + build-tools
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - name: Compile Android Kotlin
        working-directory: android
        run: |
          echo "sdk.dir=${ANDROID_HOME:-$ANDROID_SDK_ROOT}" > local.properties
          ./gradlew :app:compileDebugKotlin :app:testDebugUnitTest --no-daemon --console=plain

  desktop-compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - uses: actions/setup-java@0f481fcb613427c0f801b606911222b5b6f3083a # v5
        with:
          distribution: temurin
          java-version: "21"
      - name: Compile desktop Kotlin
        working-directory: desktop
        run: ./gradlew :composeApp:compileKotlin --no-daemon --console=plain
