name: patch-browser-popup-blocking-pr

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.head_ref == 'agent/browser-use-chromium-fixture'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          ref: agent/browser-use-chromium-fixture
          fetch-depth: 1
          show-progress: false
      - name: Restore Chromium popup blocking
        run: python scripts/_patch_browser_popup_blocking.py
      - name: Validate popup-blocking contract
        shell: bash
        run: |
          pip install -r worker/requirements.txt --break-system-packages >/dev/null
          python -m py_compile worker/app/browser_use_adapter.py tests/worker_browser_use_runtime_guard.py
          PYTHONPATH=worker python tests/worker_browser_use_runtime_guard.py
          git diff --check
          test ! -e scripts/_patch_browser_popup_blocking.py
      - name: Commit popup-blocking lock
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add worker/app/browser_use_adapter.py tests/worker_browser_use_runtime_guard.py scripts/_patch_browser_popup_blocking.py
          git commit -m "fix(research): restore Chromium popup blocking"
          git push origin HEAD:agent/browser-use-chromium-fixture
