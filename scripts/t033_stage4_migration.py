#!/usr/bin/env python3
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
PATH = ROOT / "worker/app/agent3/memory.py"
text = PATH.read_text(encoding="utf-8")


def replace_once(old: str, new: str) -> None:
    global text
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"memory.py: expected one match, found {count}: {old[:180]!r}")
    text = text.replace(old, new, 1)


replace_once(
    '''        self._lock = threading.RLock()\n        self._protector = protector or WindowsDpapiMemoryProtector()\n        self._conn = sqlite3.connect(path, check_same_thread=False)\n''',
    '''        self._lock = threading.RLock()\n        self._path = path\n        self._protector = protector or WindowsDpapiMemoryProtector()\n        self._conn = sqlite3.connect(path, check_same_thread=False)\n''',
)
replace_once(
    '''        self._conn.execute(\n            "CREATE INDEX IF NOT EXISTS idx_agent_memories_updated "\n            "ON agent_memories(updated_at DESC)"\n        )\n        self._conn.commit()\n''',
    '''        self._conn.execute(\n            "CREATE INDEX IF NOT EXISTS idx_agent_memories_updated "\n            "ON agent_memories(updated_at DESC)"\n        )\n        self._conn.commit()\n        migrated = self._migrate_legacy_sensitive_rows()\n        if migrated or self._meta_value("protection_compaction_pending") == "1":\n            self._compact_after_sensitive_migration()\n            with self._transaction():\n                self._conn.execute(\n                    "DELETE FROM agent_memory_meta "\n                    "WHERE key='protection_compaction_pending'"\n                )\n''',
)
replace_once(
    '''    def _insert_locked(\n        self,\n''',
    '''    def _meta_value(self, key: str) -> str | None:\n        row = self._conn.execute(\n            "SELECT value FROM agent_memory_meta WHERE key=?", (key,)\n        ).fetchone()\n        return None if row is None else str(row[0])\n\n    def _migrate_legacy_sensitive_rows(self) -> int:\n        """Atomically protect every non-deleted legacy sensitive row.\n\n        A mixed batch is never committed. Existing v2 rows are authenticated\n        before the store opens, while deleted tombstones remain content-free.\n        Compaction is tracked separately because VACUUM cannot run inside the\n        migration transaction; a crash between the two is resumed on reopen.\n        """\n        migrated = 0\n        with self._lock:\n            self._conn.execute("BEGIN IMMEDIATE")\n            try:\n                rows = self._conn.execute(\n                    "SELECT id,value,source_ref,schema_version,lifecycle_status "\n                    "FROM agent_memories "\n                    "WHERE sensitivity IN ('private','secret') ORDER BY id"\n                ).fetchall()\n                for row in rows:\n                    if row["lifecycle_status"] == "deleted":\n                        continue\n                    memory_id = str(row["id"])\n                    value = row["value"]\n                    source_ref = row["source_ref"]\n                    schema_version = int(row["schema_version"])\n                    if schema_version == 2:\n                        if not is_protected(value) or (\n                            source_ref is not None and not is_protected(source_ref)\n                        ):\n                            raise MemoryStoreError(\n                                "protected memory schema does not match stored fields"\n                            )\n                        self._open_field(value, memory_id=memory_id, field="value")\n                        if source_ref is not None:\n                            self._open_field(\n                                source_ref, memory_id=memory_id, field="source_ref"\n                            )\n                        continue\n                    if schema_version != 1:\n                        raise MemoryStoreError(\n                            f"unsupported sensitive memory schema: {schema_version}"\n                        )\n                    if is_protected(value) or (\n                        source_ref is not None and is_protected(source_ref)\n                    ):\n                        raise MemoryStoreError(\n                            "partial sensitive memory migration detected"\n                        )\n                    stored_value = self._seal_field(\n                        value, memory_id=memory_id, field="value"\n                    )\n                    stored_source_ref = source_ref\n                    if source_ref is not None:\n                        stored_source_ref = self._seal_field(\n                            source_ref, memory_id=memory_id, field="source_ref"\n                        )\n                    changed = self._conn.execute(\n                        "UPDATE agent_memories SET value=?,source_ref=?,schema_version=2 "\n                        "WHERE id=? AND schema_version=1",\n                        (stored_value, stored_source_ref, memory_id),\n                    ).rowcount\n                    if changed != 1:\n                        raise MemoryStoreError(\n                            "sensitive memory changed during migration"\n                        )\n                    migrated += 1\n                self._conn.execute(\n                    "INSERT OR REPLACE INTO agent_memory_meta(key,value) "\n                    "VALUES('protection_schema','2')"\n                )\n                if migrated:\n                    self._conn.execute(\n                        "INSERT OR REPLACE INTO agent_memory_meta(key,value) "\n                        "VALUES('protection_compaction_pending','1')"\n                    )\n                self._conn.commit()\n            except Exception:\n                self._conn.rollback()\n                raise\n        return migrated\n\n    def _compact_after_sensitive_migration(self) -> None:\n        if self._path == ":memory:":\n            return\n        try:\n            with self._lock:\n                self._conn.execute("PRAGMA wal_checkpoint(TRUNCATE)")\n                self._conn.execute("VACUUM")\n                self._conn.execute("PRAGMA wal_checkpoint(TRUNCATE)")\n        except sqlite3.Error as exc:\n            raise MemoryStoreError(\n                "protected memory migration compaction failed"\n            ) from exc\n\n    def _insert_locked(\n        self,\n''',
)

PATH.write_text(text, encoding="utf-8")
print("T-033 atomic legacy migration stage applied")
