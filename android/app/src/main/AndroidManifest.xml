<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Kaliv Voice: microphone capture for push-to-talk. Requested at runtime. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
      usesCleartextTraffic=true because the ModelRig backend is plain HTTP over
      the LAN. For hardening, put the backend behind Tailscale/HTTPS and drop
      this flag (or scope it with a network-security-config).
    -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/backup_rules"
        android:fullBackupContent="@xml/backup_rules_legacy"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Kaliv.Splash">
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Long-press Kaliv's launcher icon -> Planer. The explicit
                 shortcut still lands in MainActivity's authenticated rig UI. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
    </application>
</manifest>
