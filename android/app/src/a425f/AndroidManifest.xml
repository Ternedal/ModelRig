<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
          A4-25f physical probes only. This source set exists only in the
          dedicated applicationId-suffixed physical-test variant. The activities
          read that variant's separately paired rig credential from its private
          AndroidKeyStore-backed TokenStore; no credential is accepted through
          adb extras or written to physical receipts.
        -->
        <activity
            android:name=".debug.Agent4PhysicalDeviceInfoActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true" />
        <activity
            android:name=".debug.Agent4PhysicalCursorProbeActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true" />
        <activity
            android:name=".debug.Agent4PhysicalFailureProbeActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true" />
        <activity
            android:name=".debug.Agent4SnapshotPhysicalProbeActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true" />
    </application>
</manifest>
