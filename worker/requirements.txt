fastapi>=0.111,<1.0
uvicorn>=0.30,<1.0
httpx>=0.27,<1.0
pydantic>=2.7,<3.0
# Optional -- Alva Voice ASR only. NOT installed by default (keeps the base
# worker light). Install on the rig with: pip install faster-whisper
# faster-whisper>=1.0
