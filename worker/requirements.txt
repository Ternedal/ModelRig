# PINNED (==) on purpose: these are the exact versions the full worker suites
# run green against (repo analysis 1.58.40 P3 -- lock before Agent 3 grows).
# CI installs from THIS file, so tests and the released worker exe use the
# same versions. Bump deliberately: change here, run the suites, ship.
fastapi==0.139.2
uvicorn==0.51.0
httpx==0.28.1
pydantic==2.13.4
# Optional -- Alva Voice ASR only. NOT installed by default (keeps the base
# worker light). Install on the rig with: pip install faster-whisper
# faster-whisper>=1.0
# soundfile>=0.12   # decode audio without PyAV (PyAV's DLLs are blocked by
#                     # Windows Application Control on some machines)
# Optional -- Alva Voice TTS only. NOT installed by default. GPL-3.0.
# Install on the rig with: pip install piper-tts
# piper-tts>=1.4
# Optional -- RAG PDF ingest only. NOT installed by default. Extracts text from
# uploaded PDFs (PyMuPDF). Install on the rig with: pip install pymupdf
# pymupdf>=1.24
# Optional -- RAG DOCX ingest only. NOT installed by default. Extracts text from
# uploaded .docx (python-docx, paragraphs + tables). pip install python-docx
# python-docx>=1.1
